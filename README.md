# Interpretable-CNN-LSTM-ObesityRisk-SHAP
This project builds an interpretable DL model for precision obesity risk assessment. It fuses CNNs (feature extraction from multimodal data like images/vitals) with LSTMs (temporal prediction), plus SHAP for explainability. Outperforms black-box models with transparent healthcare AI insights.
